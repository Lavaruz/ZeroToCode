{
  "name": "nasa-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install":"npm install --prefix client && npm install --prefix server && npm install",
    "client": "npm start --prefix client",
    "server": "npm run watch --prefix server",
    "start": "npm-run-all --parallel server client ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
